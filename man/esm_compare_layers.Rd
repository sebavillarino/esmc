% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/esm_compare_layers.R
\name{esm_compare_layers}
\alias{esm_compare_layers}
\title{ESM comparison by soil layer (stocks)}
\usage{
esm_compare_layers(
  data,
  rvar,
  core_by,
  reference_by = NULL,
  reference_mode = c("min", "ref_trt", "custom"),
  ref_trt = NULL,
  custom_smm = NULL,
  interp = c("step", "linear", "spline"),
  rvar_unit = c("pct", "g_kg"),
  som_col = NULL,
  som_unit = c("pct", "g_kg", "frac"),
  som_from_soc = FALSE,
  soc_col = NULL,
  soc_unit = c("pct", "g_kg"),
  som_factor = 2,
  min_core_length_cm = 0,
  out_unit = c("kg_m2", "Mg_ha")
)
}
\arguments{
\item{data}{data.frame with required columns: upper, lower, bd, trt, rvar,
and grouping columns in core_by (and reference_by if not NULL).}

\item{rvar}{string. Concentration column name (e.g. "soc").}

\item{core_by}{character vector defining a soil core/profile
(e.g. c("Site","yr","trt","rep")).}

\item{reference_by}{character vector defining strata within which ESM targets
are computed (e.g. c("Site","yr")), or NULL for global targets.}

\item{reference_mode}{"min" | "ref_trt" | "custom".}

\item{ref_trt}{value in column \code{trt} used as reference treatment
(required if reference_mode = "ref_trt").}

\item{custom_smm}{custom target mineral masses (required if reference_mode = "custom").
Either:
- named numeric vector: names are boundary depths (lower), values are target cum_smm_kg_m2
- data.frame with columns: lower, target_smm_kg_m2 (and optionally reference_by cols)}

\item{interp}{interpolation method for C(M): "step", "linear", or "spline".}

\item{rvar_unit}{"pct" | "g_kg".}

\item{som_col}{optional measured SOM column.}

\item{som_unit}{"pct" | "g_kg" | "frac".}

\item{som_from_soc}{logical. If TRUE, estimate SOM from SOC using som_factor.}

\item{soc_col}{SOC column used for SOM estimation (defaults to rvar).}

\item{soc_unit}{"pct" | "g_kg".}

\item{som_factor}{numeric conversion factor (default 2).}

\item{min_core_length_cm}{numeric. Minimum profile length required.}

\item{out_unit}{"kg_m2" (default) or "Mg_ha" (adds *_Mg_ha columns).}
}
\value{
list with elements:
\itemize{
\item interval: cleaned per-layer computations (slim output)
\item profile_mass: cumulative stock vs cumulative mineral mass per core
\item layers: layer definitions
\item targets: target mineral mass per boundary
\item layer: ESM stock per layer and core
\item meta: metadata
}
}
\description{
Computes layer stocks (e.g. SOC stocks) under an Equivalent Soil Mass (ESM)
framework using the soil layers defined in the input data.
}
